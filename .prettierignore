dist/
node_modules/
src/generated/
spec/openapi.json
coverage/

# Written by release-please, which emits `*` bullets where Prettier wants `-`.
# Reformatting it would last exactly until the next release regenerates it.
CHANGELOG.md
